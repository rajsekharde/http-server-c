## Things to learn

### C Programming foundation
- Poiners & Pointer Arithmetic, Structs, Dynamic memory, string manipulation, file I/O, error handling, undefined behaviour, memory leaks

### Linux Systems Programming
- file descriptors, system calls, process vs threads, signals, blocking vs non-blocking I/O

### Computer Networking Fundamentals
- OSI model, TCP vs UDP, ports, sockets, DNS, latency vs throughput, connection lifecycle, packet fragmentation, TCP handshake

### Socket Programming in C
- TCP socket creation, binding ports, accepting connections, sending/receiving data

### HTTP Protocol
- HTTP request, response formats, headers, methods, status codes, content types, persistent connections
Stage 5 — HTTP Protocol

### HTTP Parsing
- parsing request line, parsing headers, handling CRLF, state machines, regex

### File System integration
- file metadata, directory traversal, MIME types, file streaming

### Concurrency models
- thread per connection, thread pool, event-driven I/O

### Performance engineering
- zero-copy I/O, buffer management, connection reuse, memory pools

### Security
- path traversal attacks, request size limits, rate limiting, header validation